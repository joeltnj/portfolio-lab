# nginx.conf  — pour le container Docker de l'app 2

server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Indispensable pour React Router
    location / {
        try_files $uri $uri/ /index.html;
    }
}